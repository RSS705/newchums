# Local dev API — used by `next dev`. Override in .env.local if needed.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8787
# When sharing DB with prod: set to prod API URL in .env.local so avatars load from prod R2.
# NEXT_PUBLIC_AVATAR_BASE_URL=https://newchums-api.robsmith775.workers.dev
